# Board data and source are LF everywhere: the MCP server writes from Windows and the web app
# writes from a browser, and a line-ending disagreement would show up as a whole-file diff.
* text=auto eol=lf
*.png binary
*.jpg binary
*.ico binary
