{
  "name": "@kanban/mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MCP stdio server exposing personal kanban boards to Claude Code sessions",
  "bin": {
    "kanban-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -b"
  },
  "dependencies": {
    "@kanban/core": "*",
    "@modelcontextprotocol/server": "^2.0.0",
    "zod": "^4.0.0"
  }
}
